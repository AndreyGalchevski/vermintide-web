import { VermintideTheme } from "styled-components";

const theme: VermintideTheme = {
  primary: "#212121",
  secondary: "#fafafa",
  accent: "#c62828",
  mobile: "768px",
  small: "992px",
};

export default theme;
